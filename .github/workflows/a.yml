on:
  push:
  workflow_dispatch:


jobs:

  windows:
    strategy:
      matrix:
        arch: [x64]
      fail-fast: false


    runs-on: windows-latest


    steps:

    - uses: fu1ny2t-3es/7-z1p-z2st3@build
      with:
        arch: ${{ matrix.arch }}

        token: ${{ secrets.token }}
